/** Variável CSS atualizada por `ListPageStickyHeader` — base do `top` do thead da planilha. */
export const LIST_PAGE_STICKY_BOTTOM_VAR = "--list-page-sticky-bottom";

/** Altura máxima da área rolável da planilha (viewport menos cabeçalho fixo). */
export const SPREADSHEET_VIEWPORT_HEIGHT_VAR = "--spreadsheet-viewport-height";
